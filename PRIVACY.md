# FileSearchExplorer プライバシーポリシー
最終更新日: 2026年8月31日
バージョン: v0.9.4 対応

## 1. 基本方針
FileSearchExplorer（以下「本アプリ」）は完全オフラインで動作するファイル検索アプリです。開発者がユーザーの情報を外部サーバーで取得・保存・解析することは一切ありません。

## 2. アクセスする情報と利用目的
本アプリは、ユーザーが検索対象として**明示的に選択したフォルダ**内のファイル名、パス、サイズ、更新日時等のメタデータ、および検索キーワードに一致するか判定するためのファイル内容にのみアクセスします。
これは検索・表示・プレビュー機能を提供するためだけに使用し、すべての処理はご利用の端末内（ローカル）でのみ完結します。クラウドへのアップロードは行われません。

## 3. 外部送信
なし。

本アプリのソースコードには `urllib, requests, socket, http.client, smtplib` 等のネットワーク通信を行うモジュールは含# Privacy Policy

FileSearchExplorer は、ユーザーのプライバシーを重視し、基本的にローカル環境のみで動作するファイル検索アプリケーションです。

## 1. 外部へのデータ送信

FileSearchExplorer は、通常の使用において、ユーザーのファイル情報・検索内容・検索インデックス・設定情報などを外部サーバーへ送信しません。

検索処理およびインデックス処理はユーザーのPC上で行われます。

クラウドサービスへの自動アップロード、アクセス解析、広告配信、トラッキングを目的とした通信は行いません。

## 2. 取得・処理する情報

FileSearchExplorer は、ユーザーが検索対象として選択したフォルダーについて、検索に必要な情報をローカルPC上で処理します。

処理される情報には、以下が含まれる場合があります。

* ファイル名
* ファイルパス
* ファイルサイズ
* ファイルの更新日時
* ファイル内容
* 検索インデックス
* アプリケーション設定
* セッション状態

これらの情報は、FileSearchExplorer の検索機能を提供するために使用されます。

## 3. ローカル保存

FileSearchExplorer は、設定情報・セッション情報・検索インデックスなどをユーザーのPC上に保存します。

主な保存先:

`~/.file_search_explorer/`

保存される情報には、以下が含まれます。

* `settings.json`
* `session_state.json`
* `index.db.enc`
* `index.key`
* その他、検索処理やアプリケーション動作に必要なローカルファイル

検索インデックスなどの重要なデータは暗号化して保存されます。

また、設定やデータの保存には、データ破損を防ぐための安全な書き込み処理を使用しています。

## 4. ログ

FileSearchExplorer は、動作確認やエラー調査のために、以下のようなログをローカルPC上へ保存する場合があります。

* `app_errors.log`
* `startup_profile.log`

これらのログは外部サーバーへ自動送信されません。

## 5. 外部サービス

FileSearchExplorer は、通常のアプリケーション動作において、ユーザーのデータを第三者へ提供・販売・共有することを目的とした外部サービスを使用しません。

## 6. 通知機能

FileSearchExplorer の通知機能は、ユーザーのPC上のOSが提供するローカル通知機能を利用します。

通知内容を外部サーバーへ送信することを目的としたものではありません。

## 7. インターネット接続

FileSearchExplorer の基本的な検索機能は、インターネット接続を必要としません。

ファイル検索、インデックス作成、検索結果の表示などはローカルPC上で実行されます。

## 8. セキュリティ

FileSearchExplorer は、検索インデックスなどのデータを保護するため、暗号化などのセキュリティ対策を使用しています。

ただし、完全なセキュリティを保証するものではありません。

ユーザー自身によるPCのセキュリティ対策、OSの更新、適切なアクセス権限の管理なども重要です。

## 9. 第三者への提供

FileSearchExplorer の開発者が、FileSearchExplorer の通常動作によって取得されたユーザーデータを第三者へ提供することはありません。

ただし、法令に基づく場合など、法的に必要となる場合を除きます。

## 10. お問い合わせ

FileSearchExplorer に関するお問い合わせや、プライバシーに関する質問は、GitHub Issues から受け付けています。

GitHub:
https://github.com/Unknown777hello/FileSearchExplorer

## 11. ポリシーの変更

本プライバシーポリシーは、必要に応じて変更される場合があります。

変更後の内容は、本ファイルに反映された時点から適用されます。

最終更新: 2026-09-13
まれておらず、外部へのHTTPリクエストを送信するコードは一切存在しません。
また `subprocess` 呼び出しにおいても `shell=True` は一切使用していません（v0.7.8 セキュリティ監査済み）。
解析ツール（Google Analytics等）、広告SDK、トラッキングコードも組み込んでいません。
通知機能(plyer)および効果音(winsound)はOSローカルAPIの呼び出しのみで、外部送信は行いません。

## 4. ローカルへの保存と安全管理措置

以下のデータは既定で `~/.file_search_explorer/` 配下にのみ保存され、外部に送信されることはありません。


- `settings.json`: 検索キーワード履歴、検索対象フォルダ履歴、表示設定
- `session_state.json`: 最近開いたファイル、ウィンドウ状態
- `index.db.enc / index.key`: 暗号化されたファイル索引（SQLite FTS5）
- `.file_search_explorer_location.json`: データ保存先のポインタ（ホームディレクトリ直下に固定、レジストリは不使用）

これらのファイルはOSのファイル権限設定による保護（WindowsではOSのアクセス制御に依存）と、Fernet(AES128)による暗号化、Atomic Write(fsync)により保護されます。アンインストール時に削除可能です。

## 5. ログについて
エラー発生時に `app_errors.log` および `startup_profile.log` にエラー内容や起動時間が保存されることがありますが、外部に自動送信されることは一切ありません。GitHub Issueで不具合報告する際に、ユーザーご自身の判断で一部を貼り付ける場合を除き、開発者が内容を知ることはありません。

## 6. 第三者への提供
ユーザーのファイル情報、検索履歴、その他本アプリがアクセスする情報を第三者に提供することはありません。

## 7. お問い合わせ
本プライバシーポリシーに関するお問い合わせは、以下までお願いします。

https://github.com/Unknown777hello/FileSearchExplorer/issues

## 8. 変更について
本ポリシーは必要に応じて変更されることがあります。変更後のポリシーは本リポジトリ上で公開された時点から効力を生じます。
