# FileSearchExplorer プライバシーポリシー
最終更新日: 2026年8月31日

## 1. 基本方針
FileSearchExplorer（以下「本アプリ」）は完全オフラインで動作するファイル検索アプリです。開発者がユーザーの情報を外部サーバーで取得・保存することは一切ありません。

## 2. アクセスする情報と利用目的
本アプリは、ユーザーが選択したフォルダ内のファイル名、パス、更新日時等のメタデータにアクセスします。これは検索・表示機能を提供するためだけに使用し、端末内でのみ処理されます。

## 3. 外部送信
なし。本アプリのソースコードには `urllib, requests, socket` 等のネットワーク通信を行うモジュールは含まれておらず、解析ツール（Google Analytics等）や広告SDKも組み込んでいません。

## 4. ローカルへの保存と安全管理措置
以下のデータは `~/.file_search_explorer/` 配下にのみ保存されます。
- settings.json: 検索キーワード履歴、検索対象フォルダ履歴
- session_state.json: 最近開いたファイル
- index.db.enc / index.key: 暗号化されたファイル索引

これらのファイルはOSの権限設定（0o600: 所有者のみ読み書き可）とFernetによる暗号化により保護されます。

## 5. ログについて
エラー発生時に `app_errors.log` にエラー内容が保存されることがありますが、外部に自動送信されることはありません。

## 6. お問い合わせ
[あなたのメールアドレス または GithubのIssuesのURLをここに]
